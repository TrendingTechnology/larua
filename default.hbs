<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
  <head>

    <title>{{meta_title}}</title>
    <meta name="description" content="{{meta_description}}" />

    {{! Document Settings }}
    <meta http-equiv="Content-Type" content="text/html" charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="viewport" content="width=device-width" />

    {{! Responsive Meta Tags }}

    <!-- bootstrap -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.4/css/bootstrap.min.css">

    <!-- own stylesheet for general stuff -->
    <link rel="stylesheet" href="{{asset "stylesheets/style.css"}}">

      <!-- jQuery, font-awesome and more -->
    <link href='http://fonts.googleapis.com/css?family=Raleway:400,200' rel='stylesheet' type='text/css'>

        {{! Ghost outputs important style and meta data with this tag }}
    {{ghost_head}}
</head>
<main class="{{body_class}}">

{{> header}}

   <div class="container-fluid">

       {{{body}}}

    </div>
{{#if pagination}}
    {{pagination}}
{{/if}}
</div><!-- container fluid END! -->
{{> infomodal}}
<!-- ghost footer includes jquery -->
{{ghost_foot}}

<!-- google analytics -->
{{> analytics}}

    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.4/js/bootstrap.min.js"></script>
    <script src="{{asset "javascripts/classie.js"}}"></script>

        <script>
            function init() {
            window.addEventListener('scroll', function(e){
                var distanceY = window.pageYOffset || document.documentElement.scrollTop,
                    shrinkOn = 25,
                    header = document.querySelector("header");
                if (distanceY > shrinkOn) {
                    classie.add(header,"smaller");
                } else {
                    if (classie.has(header,"smaller")) {
                        classie.remove(header,"smaller");
                    }
                }
            });
        }
        window.onload = init();
    </script>
  </main>
  <footer>
      <p class="blogdescription">Deze pagina wordt medemogelijk gemaakt door <a href="http://www.ghost.org/" target="_blank">Ghost</a>.
      De site maakt daarnaast gebruik van <a href="http://https://github.com/desandro/classie" target="_blank">Classie.js</a> voor de design elementen van de header.
      </p>
</footer>
</html>
