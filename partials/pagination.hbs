<nav class="pagination" role="pagination">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}"><i class="fa fa-arrow-left fa-2x"></i></a>
    {{/if}}
    <span class="page-number">Page {{page}} of {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}"><i class="fa fa-arrow-right fa-2x"></i></a>
    {{/if}}
</nav>
